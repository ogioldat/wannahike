{
  "name": "mcp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "zip": "rm -f lambda.zip && zip -r lambda.zip dist node_modules package.json run.sh"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^24.3.0",
    "typescript": "~5.8.3"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "fastmcp": "^3.15.1",
    "zod": "^4.1.3"
  }
}
